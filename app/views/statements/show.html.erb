<div class="container">
  <% if @current_question.nil? %>
    <div class="fak-container">
    <hr>
      <div class="fak-textcontainer">
        <strong>What you can do in this situation?</strong>
        <br>
        <br>
        <% @first_aid_kits.each do |kit| %>
          <div class="fak-text"><%=kit.content%></div>
        <br>
      </div>
      <strong>Find specialists in your area:</strong>
      <br>
        <div id="map"
          style="width: 100vw; height: 40vh;"
          data-markers="<%= @markers.to_json %>"
          data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      <% end %>
    </div>
  <%else%>
    <%=@current_question.content%>
    <%= simple_form_for @answer do |f| %>
      <%= f.association :possible_answer, label_method: :content, collection: PossibleAnswer.where(question: @current_question) %>
      <%= f.hidden_field :statement_id, value: params[:id] %>
      <%= f.button :submit %>
    <% end %>
  <% end %>
</div>
